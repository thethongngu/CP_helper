class Problem:

	def __init__(self, name = "", level = "", timeLimit = 0, memoryLimit = 0, numSample = 0, inputs = [], outputs = []):
		self.name = name
		self.level = level
		self.timeLimit = timeLimit
		self.memoryLimit = memoryLimit
		self.numSample = numSample
		self.inputs = inputs
		self.outputs = outputs
